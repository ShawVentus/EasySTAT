# EasySTAT WebUI 后端环境变量配置 (Docker 版本)

# 是否开启调试模式
DEBUG=false

# EasySTAT 项目路径 (容器内绝对路径)
EASYSTAT_PROJECT_PATH=/app/EasySTAT

# 数据总线存储路径
DATA_BUS_PATH=/app/EasySTAT/data/shared

# 结果存储路径
RESULT_DIR=/app/EasySTAT/result

# 日志存储路径
LOG_DIR=/app/EasySTAT/logs
