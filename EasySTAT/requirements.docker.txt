# ==========================================
# EasySTAT Docker 依赖清单
# ==========================================
# 合并 EasySTAT/pyproject.toml 和 easystat-webui/backend/requirements.txt

# === EasySTAT 核心依赖 ===
crewai[tools]==1.7.2
akshare>=1.13.0
tenacity>=8.2.0
joblib>=1.3.0
ta>=0.10.0
arch>=5.0.0
pandas>=1.5.0
numpy>=1.21.0
nest-asyncio>=1.5.0

# === WebUI 后端依赖 ===
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
sse-starlette>=1.6.0
python-dotenv>=1.0.0

# === 爬虫依赖（crawler.py 导入但不实际使用数据库）===
requests>=2.28.0
pymysql>=1.0.0
apscheduler>=3.10.0

# === 工具依赖 ===
aiofiles>=23.0.0
tabulate>=0.9.0
